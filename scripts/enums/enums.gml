/// enums

enum game_states {
	unready,
	preready,
	ready,
	postready
}

enum npc_types {
	employee,
	janitor
}

enum icon_types {
	normal,
	virus,
	trash
}

enum directions {
	up,
	down,
	left,
	right
}

enum transition_states {
	fade_in,
	transport,
	fade_out,
	dispose
}